
# Andrew Karpensky Portfolio

## 🚀 Final Step to Go Live

The code is now configured for a professional Vite build.

1. **Push your changes**:
   In your Codespace terminal:
   ```bash
   git add .
   git commit -m "Fix: Remove importmap and update vite config"
   git push origin main
   ```

2. **Enable the Action**:
   - Go to your repo on **GitHub.com**.
   - **Settings** > **Pages**.
   - Change **Source** to **GitHub Actions**.

3. **Watch the build**:
   - Click the **Actions** tab at the top of your repo.
   - You will see a workflow running. Once it finishes (turns green), your site is live!
